export interface MarkerData {
    lat: number;
    lng: number;
    country: string;
    city: string;
    street: string;
    name: string;
  }

  export interface ServerError {
    error: string;
  }