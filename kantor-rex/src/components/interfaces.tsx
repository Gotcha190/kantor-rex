export interface MarkerData {
    lat: number;
    lng: number;
    name: string;
    country: string;
    city: string;
    street: string;
  }

  export interface ServerError {
    error: string;
  }